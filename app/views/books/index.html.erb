<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Available Books</title>
  <link rel="stylesheet" href="styles.css"> <!-- Optional: add your CSS stylesheet link here -->
</head>
<body>
  <header>
    <h1>Available Books</h1>
  </header>

  <main>
    <% @books.each do |book| %>
      <div class="book">
        <h2><%= book.title %></h2>
        <p><strong>Author:</strong> <%= book.author %></p>
        <p><strong>ISBN:</strong> <%= book.isbn %></p>
        
        <% if book.available %>
          <%= link_to 'Borrow', borrow_book_path(book), method: :post, class: 'borrow-btn' %>
        <% else %>
          <p class="not-available">Not available</p>
        <% end %>
      </div>
    <% end %>
  </main>

  <footer>
    <p>&copy; 2025 Book Library</p>
  </footer>
</body>
</html>
